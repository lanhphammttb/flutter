import 'package:flutter/material.dart';

class InforScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Infor Screen'),
    );
  }
}
